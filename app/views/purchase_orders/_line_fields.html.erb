<div class="line-fields">
  <div class="field">
    <%= f.label :product_id, "Product" %>
    <%= f.collection_select :product_id, Current.company.products.order(:name), :id, :name, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :quantity_ordered, "Qty" %>
    <%= f.number_field :quantity_ordered, min: 1 %>
  </div>
  <div class="field">
    <%= f.label :unit_cost, "Unit cost" %>
    <%= f.number_field :unit_cost, step: 0.01, min: 0 %>
  </div>
  <% if f.object.persisted? %>
    <div class="field">
      <label>&nbsp;</label>
      <%= f.check_box :_destroy %> <%= f.label :_destroy, "Remove" %>
    </div>
  <% end %>
</div>
